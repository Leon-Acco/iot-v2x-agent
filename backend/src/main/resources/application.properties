# capability sql_template contains Doris template placeholders (e.g. time_from placeholders);
# disable Flyway placeholder replacement so V19 seed migration parses correctly.
spring.flyway.placeholder-replacement=false
