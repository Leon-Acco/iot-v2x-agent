server.port=8080
# service name (also the Nacos dataId prefix)
spring.application.name=dst-v2x-agent
# active profile -> config/bootstrap-{profile}.yml; override via SPRING_PROFILES_ACTIVE
spring.profiles.active=local
